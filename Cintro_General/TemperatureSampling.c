#include <stdio.h>
#include <string.h>

int main() {
    int A, S;
    int location, time_point;
    float temp;

    scanf("%d %d", &A, &S);
    while (1) {
        scanf("%d %d %d:%d:%d", &location, &temp, &time_point);
        
    }

}